namespace Linq.Samples.Models
{
    public enum AccountType { Checking, Investiments, Other }
    public enum CategoryType { Expense, Earnings }
    public enum RepeatType { NoRepetition, FixedExpense, ExpenceInInstallments }
}
